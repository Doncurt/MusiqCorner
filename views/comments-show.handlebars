<a href="/">Back to Home</a>
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
      <h1>{{comments.video}}</h1>
      <h2>{{comments.name}}</h2>
      <p>{{comments.comment}}</p>
    </div>
  </div>
